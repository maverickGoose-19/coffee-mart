# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database — psycopg3 with binary driver and connection pooling
psycopg[binary,pool]>=3.2.0

# Config / settings
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Email notifications
resend>=2.5.0

# --------------------------------------------------------------------------
# Dev / test
# --------------------------------------------------------------------------
pytest>=8.0.0
httpx>=0.27.0          # required by FastAPI TestClient
